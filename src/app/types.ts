
  
  export enum ViewMode {
    Grid = 'grid',
    List = 'list',
  }
  export interface DataRecord {
    Poster: string;
    Title: string;
    Type: string;
    Year: string;
    imdbID: string;
  }
  export enum SortDirection {
    Asc = 'ascending',
    Desc = 'descending',
  }
  
  export interface DataRecord {
    Poster: string;
    Title: string;
    Type: string;
    Year: string;
    imdbID: string;
  }
  
